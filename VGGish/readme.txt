
利用write_lst.py 生成数据列表

所有功能在train_audio.py里面

程序本身就对数据进行了扩充。对于输入每一段音频，以1s为单位，通过选不同音频开始点，得到对应的10段1s的10个音频文件。

有两种方式处理，第一首先提取特征

1. 运行extract_feature函数，该函数功能是输入data，得到数据的特征.npy格式
2. train_fc_audio函数，输入特征文件，训练分类器

第二种方式输入是音频文件，输出是分类结果，是一个端到端的网络
运行train_fc_audio（）

准确率结果保存在result文件夹下

